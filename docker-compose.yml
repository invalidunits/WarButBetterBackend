services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: war-but-better-backend:latest
    container_name: war-but-better-backend
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_URLS: http://+:8080
    restart: unless-stopped
